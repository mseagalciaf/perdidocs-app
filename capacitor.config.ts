import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jamedev.perdidocs',
  appName: 'perdidocs',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
